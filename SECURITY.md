# Security

PatchWitness executes local commands from task files and repositories.

Use it only with repositories and tasks you trust. Do not run unknown tasks on a
machine that contains sensitive files, credentials, active cloud sessions, or
important local services.

Do not report sensitive vulnerabilities in a public issue. Open a minimal issue
asking for a private security contact, without exploit details or secrets.
